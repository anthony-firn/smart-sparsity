# General dependencies
kivy==2.1.0
psutil==5.8.0
torch==2.0.1
transformers==4.25.1  # Update to a version with fewer known issues

# Optional dependencies for building the app (e.g., PyInstaller)
pyinstaller==4.5.1

# Testing dependencies
unittest2==1.1.0